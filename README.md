# Budget Tracker

Backend-сервис для приложения учета личных финансов.  
Система позволяет пользователям структурировать доходы, расходы, управлять категориями и получать аналитику с авторизацией.

## О проекте

Цель проекта — создать масштабируемый, удобный для поддержки и безопасный REST API для:

- регистрации и входа пользователей
- управления категориями доходов и расходов
- хранения финансовых операций
- получения статистики

## Архитектура

Проект использует **Чистую архитектуру (Clean Architecture)**.

src/main/java/com/example/budget

api/            - REST-контроллеры и DTO  
domain/         - Бизнес-сущности  
application/    - Сервисы и сценарии работы
infrastructure/ - Работа с БД, безопасность, Spring

## Sprint Review

По итогам спринта команда получила:

- Поднята инфраструктура проекта (Spring Boot, структура пакетов)
- Настроено подключение к PostgreSQL
- Подготовлен слой API (контроллеры и DTO)
- Подключена JWT-авторизация

## Sprint Retrospective

Что прошло хорошо:

- Архитектура была выбрана правильно
- Проект удалось запустить и подключить базу данных
- Команда быстро разобралась с ролями
- Удалось внедрить JWT и безопасную авторизацию

Что пошло не так:

- Сложность архитектуры оказалась выше, чем ожидалось
- Некоторые задачи были оценены ниже, чем предполагалось


## ЗАПУСК
1) При открытом докере запустить в терминале команду
```
docker rm -f budget-db 2>/dev/null || true
docker run --name budget-db \
  -e POSTGRES_DB=budget \
  -e POSTGRES_USER=budget \
  -e POSTGRES_PASSWORD=budget \
  -p 5433:5432 \
  -d postgres:15
```
2)Запустить .jar из release
```
cd ~/Downloads
java -jar budget-tracker-1.0.0.jar
```
3)Зайти на
- http://localhost:8080/ - UI Interface
- http://localhost:8080/swagger-ui/index.html#/ - Отладка REST API
